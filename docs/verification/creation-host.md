# 创作升级宿主验收 · 2026-09-12

## 环境与范围

实际环境：macOS、Obsidian 1.13.7，独立测试库 `/private/tmp/md2wechat-creation-vault`。使用自建验收文章与图片；未创建真实微信草稿，未正式发布文章或软件版本。

已实际查看内置浅色、深色主题，以及约 288 和 480 像素的侧栏截图。这里的宽度是本轮观察值，不代表原计划中 320/360/480/720 四个精确宽度均已验收。

## 已完成的真实操作

| 操作 | 观察结果 |
| --- | --- |
| Codex 连接 | 在插件连接窗口中真实连接成功 |
| 智能增强首次运行 | 真实生成候选；发现重点提示缺字段，修复后取消该候选，未将有问题的候选作为验收通过结果 |
| 智能增强复验 | 再次真实生成；编号步骤、重点提示、对比展示及文章既有图片正常，确认采用成功 |
| 生成期间切换文章 | 切到第二篇后，采用第一篇候选未让第二篇出现第一篇内容；回到第一篇，增强和图片仍在 |
| 标题批次 | 真实生成两批，每批 10 个；选择第一批第一个，换到第二批，再回第一批，原选择仍标为已选 |
| 关闭重载 | 标题、封面、正文图片和已采用增强均保留 |
| 封面选择 | 使用此前真实生图的图片，经生产候选保存方法注入测试候选；在界面选择后保存到文章的 `./资源` 附件位置 |
| 封面裁剪 | 在界面选择方形裁剪，另存为新图片并选中，原候选未覆盖 |
| 创建窗口预填 | 最终创建窗口自动带入已选标题和裁剪封面；未点击最终确认 |
| 正文配图 | 在界面选择既有验收图片，插入第一段后；刷新排版及切换 Apple 主题后图片仍在 |
| 润色比较与采用 | 默认“清楚简洁”，真实 Codex 返回改写候选；展开调整前内容，前后均为排版预览，既有图片、步骤、提示、对比保留；采用后提示成稿已采用、原文未改动 |
| 撤回上次排版 | 在“更多”中撤回后，正文恢复原措辞，图片及增强保持；已查看截图 |
| 发布前检查 | 实际检查通过，随后重新打开创建窗口核对资料；检查没有创建草稿 |
| 图片方案目录 | 界面读取真实目录：封面 14 项、正文配图 25 项；选择 Apple 封面方案和手绘正文方案，读取测试配置确认仅对应两个方案字段保存成功；没有再次付费生图 |

封面候选测试使用的是先前通过真实生图验证得到的文件，不能描述为“本轮在界面一次生成了两张封面”。正文插图使用既有文件，不能描述为正文配图服务的真实付费生成验证。独立生图证据见 [本地图片生成验证](creation-image.md)；Claude 经用户现有 zhipu 环境的独立任务证据见 [创作助手验证](creation-agent.md)。

## 原文与文章隔离

第一篇原文最终 SHA-256：`ba3e94153cc8b3d46f07e56b265cab23c877a0c19e5f55c6ab174a8c1ccaea24`。原文没有加入 `:::` 模块或 Markdown 图片标记 `![]`；增强与插图只进入公众号成稿。第二篇文章未改变。

## 自动检查与安装包

最新完整检查：139 项通过，1 项需主动开启的测试跳过。真实五类模块检查另外单独运行并通过，不把跳过项写为已经由默认检查执行。界面中的真实增强复验覆盖步骤、提示、对比三个模块，不能据此声称五类都经过宿主逐项操作。

已运行 `npm run package`。检查所得 ZIP：16 个条目、127,255 字节；三个必需插件文件及配套技能齐全，不含 `data.json` 或测试资料。此记录是本地包检查，不代表已上传 GitHub Release，也不代表已下载核验发布后的包。

## 尚未完成的验证与当前范围

尚未在宿主实际操作：全部精确宽度、大字号、第三方主题、四种附件位置。未验证 Windows、Linux、移动端，也未创建真实微信草稿。没有进行初学者独立使用研究，不能给出体验评分或成功率。

当前增强支持五类有限展示；使用段落选择和候选对照查看、调整结果。原规划中的直接跳转正文位置、通用高级提示词编辑器不属于本轮收敛交付范围；它们是未来可评估项，不应列为当前已实现功能，也不应当作本轮运行阻碍。当前图片生成仅支持用户现有 md2wechat 火山配置。
