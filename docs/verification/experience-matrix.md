# 候选版验证记录

环境：2026-09-08，macOS，隔离 Obsidian 1.12.7 / 安装器 1.12.7，md2wechat 3.5.0。CLI 仓库没有改动，现用 Obsidian 安装与真实笔记库没有替换。

| 检查 | 结果与证据 |
| --- | --- |
| 未保存内容 | 官方 CLI 捕获 EDITOR_VERSION，磁盘保持 DISK_VERSION；见 native-bridge.md |
| 正式主流程 | 正式 capture → 当前 md2wechat preview → 正式 present 通过，实际界面显示 3168 字节文章 HTML |
| 中文图片与 wiki 引用 | 正式回环通过，归一副本资源并显示，原文不改 |
| 十种文章样本 | 真实 CLI inspect 通过，包括长文、列表、表格、代码、中文路径、引用式图片 |
| 发布前资料检查 | 真实 inspect --draft 返回 draft_ready=true，无上传或草稿副作用 |
| 最终确认 | 原生界面 + 完全离线的虚构账号程序：确认前零上传/草稿，确认后一次草稿调用；返回 fixture-draft |
| 内容不变 | 图片替换单元检查仅修改 src；正文文字、style、其他属性原样保留 |
| 防重复和不确定结果 | 独立调用、并发、重复确认、断线后重试均无第二次创建 |
| 恢复和请求顺序 | 新建结果存储实例恢复；晚到结果不覆盖；原文变化保留旧版并阻止创建 |
| 原生外观 | 已打开实际默认浅色界面，发现并修正状态栏遮挡底部按钮；后续检查另记 |

## 真实发现与处理

- CLI 本机配置指向未运行的 localhost:3000。测试仅通过进程环境使用官方转换地址，未修改 CLI 配置。真实使用仍需要现有 md2wechat 配置能成功预览。
- CLI inspect 把代码块里的图片写法当成真实素材，实际 preview 正常。插件只在发布检查副本里用成熟解析器排除代码内容；渲染稿与已审阅 HTML 不改。没有复制 CLI 的资料或上传规则。
- 官方 registerCliHandler 不自动加插件命名空间，现已显式注册完整命令名。

## 尚未验收

真实公众号草稿、公众号编辑器粘贴、五种 Agent 的独立首次接入、Windows/Linux、最新稳定安装器、10 位用户测试和双人盲评。未完成项不能折算为通过，也不宣称体验已达 90 分。

## 后续实际界面检查

- 进程退出并重新启动隔离 Obsidian 后，上次图片成品自动恢复。
- Minimal 深色主题，面板宽度 320/360/420/640/900 像素均无水平溢出；按钮底部距窗口底部 38 像素，正文占面板高度约 73%–75%（960 像素高测试区域）。
- 保留原插件视图标识，避免升级后旧排版标签无法恢复。

- 原生主题搜索切换真实完成，富文本剪贴板读取含 HTML 正文；公众号网页编辑器粘贴尚未测试。
- 实际修改文章后，旧预览保持且创建按钮禁用；撤销修改后恢复可用。
- 45 项自动检查通过，包含 10 种真实 CLI 文章检查。

![原生预览](images/preview.png)

- 默认深色、Minimal 浅色同样完成五档宽度检查，无水平溢出或被遮住的创建按钮。
