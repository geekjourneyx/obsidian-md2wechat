> 历史记录：本文保留 2.0 阶段的设计或验收，不作为 2.1 当前操作指南；当前说明见 README、INSTALL 与 docs/FIRST-RUN.md。

> 2.0 发布验收档案：按时间追加，早期候选版状态由后续实测记录覆盖。当前范围见 README 与 AGENT-COMPATIBILITY。

# 候选版验证记录

环境：2026-09-08，macOS，隔离 Obsidian 1.12.7 / 安装器 1.12.7，md2wechat 3.5.0。CLI 仓库没有改动，现用 Obsidian 安装与真实笔记库没有替换。

| 检查 | 结果与证据 |
| --- | --- |
| 未保存内容 | 官方 CLI 捕获 EDITOR_VERSION，磁盘保持 DISK_VERSION；见 native-bridge.md |
| 正式主流程 | 正式 capture → 当前 md2wechat preview → 正式 present 通过，实际界面显示 3168 字节文章 HTML |
| 中文图片与 wiki 引用 | 正式回环通过，归一副本资源并显示，原文不改 |
| 十种文章样本 | 真实 CLI inspect 通过，包括长文、列表、表格、代码、中文路径、引用式图片 |
| 发布前资料检查 | 真实 inspect --draft 返回 draft_ready=true，无上传或草稿副作用 |
| 最终确认 | 原生界面 + 完全离线的虚构账号程序：确认前零上传/草稿，确认后一次草稿调用；返回 fixture-draft |
| 内容不变 | 图片替换单元检查仅修改 src；正文文字、style、其他属性原样保留 |
| 防重复和不确定结果 | 独立调用、并发、重复确认、断线后重试均无第二次创建 |
| 恢复和请求顺序 | 新建结果存储实例恢复；晚到结果不覆盖；原文变化保留旧版并阻止创建 |
| 原生外观 | 已打开实际默认浅色界面，发现并修正状态栏遮挡底部按钮；后续检查另记 |

## 真实发现与处理

- CLI 本机配置指向未运行的 localhost:3000。测试仅通过进程环境使用官方转换地址，未修改 CLI 配置。真实使用仍需要现有 md2wechat 配置能成功预览。
- CLI inspect 把代码块里的图片写法当成真实素材，实际 preview 正常。插件只在发布检查副本里用成熟解析器排除代码内容；渲染稿与已审阅 HTML 不改。没有复制 CLI 的资料或上传规则。
- 官方 registerCliHandler 不自动加插件命名空间，现已显式注册完整命令名。

## 尚未验收

真实公众号草稿、公众号编辑器粘贴、五种 Agent 的独立首次接入、Windows/Linux、最新稳定安装器、10 位用户测试和双人盲评。未完成项不能折算为通过，也不宣称体验已达 90 分。

## 后续实际界面检查

- 进程退出并重新启动隔离 Obsidian 后，上次图片成品自动恢复。
- Minimal 深色主题，面板宽度 320/360/420/640/900 像素均无水平溢出；按钮底部距窗口底部 38 像素，正文占面板高度约 73%–75%（960 像素高测试区域）。
- 保留原插件视图标识，避免升级后旧排版标签无法恢复。

- 原生主题搜索切换真实完成，富文本剪贴板读取含 HTML 正文；公众号网页编辑器粘贴尚未测试。
- 实际修改文章后，旧预览保持且创建按钮禁用；撤销修改后恢复可用。
- 45 项自动检查通过，包含 10 种真实 CLI 文章检查。

![原生预览](images/preview.png)

- 默认深色、Minimal 浅色同样完成五档宽度检查，无水平溢出或被遮住的创建按钮。

## 2026-09-09 真实公众号草稿验收

用户明确授权后，在现用 Obsidian 1.13.7 中通过已安装插件 2.0.0-beta.1 实际点击预览、选择测试封面、最终确认。现有本地服务启动后转换成功，服务地址未更改。

- 标题：插件草稿验收测试（请勿发布）。
- 真实封面上传和草稿创建成功，返回非空草稿编号；插件持久记录 completed，界面显示“草稿已创建”。
- 本次只有一份创建记录，未执行正式发布，也未重复创建。
- 这是原生插件手动预览到草稿的真实验收；不等同于所有 Agent 宿主、公众号网页编辑器粘贴或正式发布验收。
- 私有草稿回执保存在本地 artifacts，未写入公共文档。

上文“真实公众号草稿尚未验收”为前一日状态，此项现已通过。

### 2026-09-09：创建成功后的收尾体验

- 成功后自动关闭确认框，并刷新排版面板；保存偏好失败不影响成功结果。
- 面板读取已有完成记录，提供真实草稿链接；缺少链接时明确显示“打开公众号后台”。不根据草稿编号拼接地址。
- 已在当前 Obsidian 重启后核对测试文章：完成状态保留，单账号不再显示重复创建按钮；已点击后台入口。
- 本轮复用已有草稿，无新增上传或草稿创建。
- `npm run check`：49 项检查通过，构建通过。新增成功关闭及偏好保存失败的行为测试；链接校验与跨文章、跨账号隔离测试通过。

### 2026-09-09：顶部操作区与原文刷新

- 删除重复的“公众号排版”眉题；文章名采用原生界面字体及单行截断。
- 创建草稿、查看后台统一置于顶部操作区；字号、复制收进原生“更多”菜单，窄栏时主操作独立一行。
- 原文修改后提供“刷新排版”，读取最新原文并沿用当前主题、字号，不自动上传或创建草稿；微信后台的修改不会反向同步。
- 当前 Obsidian 实测：点击刷新后，预览显示用户新增的“正文可以正常阅读 11”，过期提示消失；更多菜单包含复制及三个字号选项。
- 50 项检查与构建通过；新增检查确保刷新不复用旧排版正文。未新增微信草稿。

### 2026-09-09：草稿记录与主题兼容

- 按文章读取各次排版已有的草稿完成记录，显示公众号、创建时间及当前/旧版本区别；未找到记录时说明“尚无创建草稿记录”。不声称微信后台仍保留草稿。
- 新增跨文章、跨账号、重新排版后保留历史的检查，51 项检查与构建通过。
- 查明 Baseline 主题直接清除侧栏子 div 内边距；将顶部控件放入独立语义区域，保留插件边距，统一按钮 32px 高度、对齐与焦点提示。
- Baseline 实际界面复核：左右按钮居中对齐，上下及右侧留白恢复；普通文章显示“尚无创建草稿记录”，切回测试文章显示“旧版本曾创建草稿”、公众号及时间，并保留后台入口。未创建新草稿。
- 修复桌面插件环境不支持动态导入导致历史记录静默缺失的问题，改用现有静态导入方式；最终 51 项检查与构建再次通过。
